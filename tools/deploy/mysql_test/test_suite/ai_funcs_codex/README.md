# AI functions extension tests

This suite contains additional coverage added with the Document AI Functions
and IK custom dictionary implementations. It is intentionally separate from
`ai_funcs` so the supplied acceptance tests remain unchanged.

Run it with the same mysqltest runner used for other suites, selecting
`ai_funcs_codex` as the suite name.
