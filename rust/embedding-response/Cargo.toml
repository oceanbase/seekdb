[package]
name = "embedding-response"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0 AND MIT"
description = "Compatible, memory-safe parsing of seekdb embedding responses."

[dependencies]
