load("//bazel:defs.bzl", "seekdb_module_cc_test")

package(default_visibility = ["//visibility:private"])

seekdb_module_cc_test(
    name = "observer_tests",
    srcs = glob(["**/*.cpp"]),
    test_hdrs = glob(
        [
            "**/*.h",
            "**/*.hh",
            "**/*.hpp",
            "**/*.ipp",
        ],
        allow_empty = True,
    ),
    module_interface = "//src/observer:observer_unit_test_interface",
    unity_exceptions = {
        'omt/test_geo_srs_parser.cpp': 'rewrites C++ access control before including production headers',
    },
    shard_count = 4,
)
