load("//bazel:defs.bzl", "seekdb_module_cc_test")

package(default_visibility = ["//visibility:private"])

seekdb_module_cc_test(
    name = "rootserver_tests",
    srcs = glob(["**/*.cpp"]),
    test_hdrs = glob(["**/*.h"], allow_empty = True),
    module_interface = "//src/rootserver:rootserver_unit_test_interface",
    unity_exceptions = {
        'test_ddl_task_struct.cpp': 'rewrites C++ access control before including production headers',
    },
    shard_count = 4,
)
